# Supabase Edge Functions

All Edge Functions live inside this directory.

- Generate new functions with the Supabase CLI (e.g., `supabase functions new <name>`).
- Use modules from `infra/lib/` for shared logic.
- Deploy or test functions via commands run from the `infra/` directory.