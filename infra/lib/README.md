# infra/lib

Shared utilities and domain helpers for Supabase Edge Functions and other infrastructure code live here.

- Add reusable TypeScript modules (e.g., database adapters, validation helpers).
- Import these modules from functions under `infra/supabase/functions/` or other infra tooling.